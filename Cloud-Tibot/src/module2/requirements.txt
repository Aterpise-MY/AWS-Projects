# Lambda Requirements for Module 2: Git Radar
# GitHub Copilot SDK with GitHub App Authentication

# Core dependencies
urllib3>=2.0.0
boto3>=1.28.0

# GitHub App JWT Authentication
PyJWT>=2.8.0
cryptography>=41.0.0

# Note: Lambda runtime already includes boto3, but we specify minimum version for compatibility
